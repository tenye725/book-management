version: 1
applications:
  - appRoot: book-management-front
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../book-management-front
            - npm ci --cache .npm --prefer-offline
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - .npm/**/*